# Workspace Rules

## Git & Deployment Guidelines
- **Do NOT automatically push code to GitHub** unless explicitly instructed by the user. Make changes locally and ask or wait for the user's approval before executing `git push`.
